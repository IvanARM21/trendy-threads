// import { menCategories, womenCategories } from "@/data/filters";

// interface Props {
//   gender: string;
// }

export const FiltersContainer = () => {
  // const categories = gender === "men" ? menCategories : womenCategories;

  return (
    <form className="col-span-1 hidden xl:flex flex-col sticky top-20 h-80">
      {/* <FilterWidget options={categories} slug="category" title="Categorías" />

      <FilterWidget options={colors} slug="color" title="Colores" />

      <FilterWidget options={sizes} slug="size" title="Tallas" /> */}
    </form>
  );
};
